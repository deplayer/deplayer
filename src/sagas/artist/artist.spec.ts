import { describe, it } from "vitest";

// TODO: Tests need to be updated for LiveStore migration
// The fetchSongMetadata function no longer uses the repository pattern
// and now directly integrates with LiveStore for lyrics management

describe("Artist Saga", () => {
  describe("fetchSongMetadata", () => {
    it.todo("Update tests for LiveStore migration - lyrics no longer use repository pattern");
  });
});
